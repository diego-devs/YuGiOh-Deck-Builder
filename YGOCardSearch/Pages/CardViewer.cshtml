@page
@model YGOCardSearch.Pages.CardViewerModel
@{
    ViewData["Title"] = "CardViewer";
}


<div class="card mb-3" style="max-width: auto;">
    <div class="row no-gutters">
        <div class="col-md-4">  
            <div class="inner"><img src="@Model.Card.CardImages[0].ImageUrl" class="card-img" alt="imagehere"></div>
                
        </div>
        <div class="col-md-8">
            <div class="card-body"> 
                <table class="table table-dark table">
                    <tr>
                        <td>
                            <p class="card-text">@Model.Card.Type</p>
                        </td>
                        <td>
                            <p class="card-text">@Model.Card.Attribute</p>
                        </td>
                        <td>
                            <p class="text-white">@Model.Card.Race</p>
                        </td>

                    </tr>
                </table>
                <h5 class="card-title text-center">@Model.Card.Name</h5>
                
                <p class="card-text">@Model.Card.Desc</p>
                <p class="card-text"><small class="text-muted">$ @Model.Card.CardPrices[0].TcgplayerPrice.ToString() USD</small></p>
                <div class="card">
                    @{
                        foreach (var set in Model.Card.CardSets)
                        {
                            <div class="card mb-4" style="max-width: auto;">
                                <div class="row d-table-row">
                                    <p class="card-text">@set.SetName</p>
                                    <p class="card-text">@set.SetRarity</p>
                                    <p class="card-text">@set.SetPrice</p>
                                </div>

                            </div>
                           
                        }

                    }
                </div>
                <div class="flex-xl-row">Card Id: @Model.Card.Id</div>
               
                

            </div>
        </div>
    </div>
</div>
